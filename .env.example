# ===========================================
# Desire Comfort – Store (Stripe + Supabase)
# Add these in .env (local) and Vercel → Environment Variables (production).
# Never commit .env or paste real keys in chat.
# ===========================================

# Stripe – checkout + webhook
# Dashboard → Developers → API keys → Secret key
STRIPE_SECRET_KEY=

# Stripe webhook – so we can save orders to Supabase
# Dashboard → Developers → Webhooks → Add endpoint → Signing secret
# Production URL: https://your-site.vercel.app/api/stripe-webhook
STRIPE_WEBHOOK_SECRET=

# Supabase – backend database for orders
# Project → Settings → API → Project URL
SUPABASE_URL=

# Project → Settings → API → service_role (secret). Server only, never in frontend.
SUPABASE_SERVICE_ROLE_KEY=

# Optional: only if frontend calls API from another origin during local dev
# VITE_API_BASE=
